# PulseCheck
ITTool is a lightweight Windows troubleshooting tool for support engineers. It provides a simple UI for running common diagnostic checks on a user machine, saves every task result in one structured run folder, and generates a readable HTML report that can be attached to a ticket or reviewed later.
